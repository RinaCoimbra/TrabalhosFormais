# aiu diuashd uiash diuas
2+2
BEGIN
{	
	asort(a, asd);
	asort("a", asd);
	asort(a, "asd");
	asort(a);
	asort("a");

	do
	{
		break;
	}
	while(a+b)

	exit;

	atan2(2, 2);
	atan2(2, 2);

	while (a+b) {
		do
		{
			atan2(2, 2.3);
		}
		while(a)
		break;
	}

	for (a; ; 0)
	{
		b - 2;
		exit a+2;
		for (a+=0; a+a; 0)
		{
			cos(a+v);
			cos(a+v);
			a = 1;
			break;
		}	
	}

	cos(a+v);

	if(a+=8)
		b+2;

	if (a+b)
	{	
		delete a;
		if (a+b)
		{
			if (a+b)
			{
				while (a+b)
				{

				}

				do
				{
					for (a+=0; a+a; 0)
					{
						a+b;
						break;
						a = 1;
						continue;
					}	
					continue;
				}
				while(a)
			}
		}

		else
		{
			cos(a+v);
			if (a+b)
			{
				delete a[3];
				atan2(2, 2);
			}
		}

		exp(a+s-2);
	}

	else
	{
		while (a+b)
		{
			exp(a+s-2);
			index(a, d);
			index(3, 4);
			index(a+3, 4);
		}
	}
}

asort("a")
asorti("a")
2+3
exp(a+s-2)
2+3
2+3
cos(a+v)
asort(a, asd)
asorti(a, asd)
asort("a", asd)
asorti("a", asd)
asort(a, "asd")
asort(a)
length()
length(a)
length(a+3)


BEGIN{
	for (a+=0; a +a; 0)
	{
		a = 1;
		continue;
	}	
}
2+3

BEGIN{
	v[0, 1, 3.2]
}

{	
	if (a+b)
	{	
		if (a+b)
		{
			if (a+b)
			{
				int(1.3)
				if (a+b)
				{

				}
			}

			else
			{	
				for (a+=0; a+a; 0)
				{
					continue;
					a = 1;
					sin(a+2.3);
				}	

				int (a+2.3)
			}
		}
	}

	else
	{
		while (a+b)
		{
			rand();
			split(a, b);
			split(a+2, b);
			split(2, b);
			strtonum(a);
			strtonum(3);
			strtonum(3+e);
		}
	}

	rand();

	while (a+b)
	{
		substr(a, s, r);
		substr(a, s+3, 2);
		substr(a, s, r);
		substr(a, s);
		substr(a+4, s);
	}
}

getline;
getline a+b;
getline 6;
print;
print a+b;
print 6;


rand();
BEGIN{}

atan2(2, 2);
atan2(2, 2);

BEGIN{}

{}

atan2(2, 2);
log(1.2+3/abc);
sin(a+2.3);
int (a+2.3);
srand(3+2);
srand(s*d);

{}
{}

tolower(a);
tolower(a+v);
tolower(9);
toupper(a);
toupper(a+v);
toupper(9);
2+3;
atan2(2, 2);
2+3,;
2+3;
atan2(2, 2);
atan2(2, 2);

END
{	
	if (a+b)
	{
	}

	else
	{
		sin(a+2.3);
		srand();
	}
}

END {}
END {}
srand();
BEGIN{}
END {}
sqrt(awk+2);
{}
{}
END {}